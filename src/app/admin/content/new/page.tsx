import { ContentForm } from "@/components/admin/content-form";

export const metadata = {
  title: "New content · Admin",
};

export default function AdminNewContentPage() {
  return <ContentForm />;
}
